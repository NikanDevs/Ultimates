import { interactionOptions } from '../../typings/Command';

// @ts-ignore
export const userinfoContextmenu = {
	name: 'User Info',
	description: 'Shows information for a user',
	directory: 'utility',
	cooldown: 3000,
	permission: ['ManageMessages'],
	type: 2,
} as interactionOptions;

